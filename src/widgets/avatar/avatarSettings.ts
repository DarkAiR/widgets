import {ISettings} from "../../interfaces";
// import {ServerType} from "../models/types";

export interface AvatarSettings extends ISettings {
    /* title: string;
    server: ServerType;

    value1Style: string;
    value2Style: string; */
}
